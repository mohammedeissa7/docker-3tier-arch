name: notebooks-backend
services:
  notebooks-backend:
    build: 
      context: .
      dockerfile: dockerfile.dev
    ports:
        - "3000:80"
    environment:
        - PORT=80
    develop:
      watch:
          - action: sync
            path: ./src
            target: /app/src  
  